#!/usr/bin/env bash
# ~/.local/bin/rofi-ssh — Interactive SSH Session Launcher

rofi -show ssh -terminal foot -ssh-command "foot -c $HOME/.config/foot/ssh.ini --app-id=foot.ssh -T '󰢹 SSH: {host}' -e ssh {host}" -display-ssh "󰢹 SSH"
