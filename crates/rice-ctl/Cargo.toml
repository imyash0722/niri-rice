[package]
name = "rice-ctl"
version = "0.3.0"
edition = "2021"
authors = ["imyash0722"]
description = "High-performance unified desktop controller & native theming engine for Niri rice"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"
rand = "0.8"
