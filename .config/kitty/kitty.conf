# Kitty Configuration - Niri Rice
# Dynamic Material You theming via colors.conf (end-4 style)

include ./colors.conf

# ── Font & Typography ──
font_family      JetBrainsMono Nerd Font
bold_font        auto
italic_font      auto
bold_italic_font auto
font_size        12.0

# ── Cursor & Animation ──
cursor_shape     beam
cursor_trail     1

# ── Window Styling & Translucency ──
background_opacity         0.90
background_blur            1
dynamic_background_opacity yes
window_padding_width       14
confirm_os_window_close    0
hide_window_decorations    yes
remember_window_size       no
initial_window_width       960
initial_window_height      600

# ── Shell & Session ──
shell                      /usr/bin/zsh
close_on_child_death       no

# ── Remote Control (for live Matugen palette reload) ──
allow_remote_control       yes
listen_on                  unix:/tmp/kitty

# ── Clipboard & Copy/Paste ──
map ctrl+c                 copy_or_interrupt
map ctrl+v                 paste_from_clipboard

# ── Keybindings for Tmux Fast Tab Switching ──
map ctrl+tab               send_text all \x1b[27;5;9~
map ctrl+shift+tab         send_text all \x1b[27;6;9~

# ── Scroll & Zoom ──
map page_up                scroll_page_up
map page_down              scroll_page_down

map ctrl+plus              change_font_size all +1
map ctrl+equal             change_font_size all +1
map ctrl+kp_add            change_font_size all +1
map ctrl+minus             change_font_size all -1
map ctrl+underscore        change_font_size all -1
map ctrl+kp_subtract       change_font_size all -1
map ctrl+0                 change_font_size all 0
map ctrl+kp_0              change_font_size all 0

# ── URLs & Mouse ──
url_style                  curly
open_url_with              default
detect_urls                yes
copy_on_select             no
strip_trailing_spaces      smart
mouse_hide_wait            3.0
enable_audio_bell          no
visual_bell_duration       0.0
